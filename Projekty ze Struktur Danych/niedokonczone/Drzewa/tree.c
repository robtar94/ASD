#include <stdio.h>
#include "tree.h"
void init(struct tree *T) {
	T->root = NULL;
}
int rand (int a, int b)
{
	int rand_walk_tree (Tree *T) {
		struct node *x;
		struct node *y;
		x = tree-> root;
		y = y ->Null;
		
	}
}
